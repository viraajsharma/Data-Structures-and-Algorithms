class Solution(object):
    def getConcatenation(self, nums):
        arr = []
        for num in nums:
            arr.append(num)
        for num in nums:
            arr.append(num)
        return arr